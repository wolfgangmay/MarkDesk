<Window x:Class="MarkDesk.Controls.ThemedMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        MinWidth="360" MaxWidth="520"
        Background="{DynamicResource WindowBgBrush}"
        Title="">
    <Grid Margin="24,20,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="IconHost" FontFamily="Segoe MDL2 Assets"
                       FontSize="28" VerticalAlignment="Top" Margin="0,0,16,0"
                       Foreground="{DynamicResource AccentBrush}" />
            <TextBlock Grid.Column="1" x:Name="MessageText" TextWrapping="Wrap"
                       Foreground="{DynamicResource ContentBrush}" FontSize="14" LineHeight="22" />
        </Grid>

        <StackPanel Grid.Row="1" x:Name="ButtonPanel" Orientation="Horizontal"
                    HorizontalAlignment="Right" />
    </Grid>
</Window>
